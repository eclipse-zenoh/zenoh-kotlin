name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]
  schedule:
    - cron: "0 6 * * 1-5"
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ["${{ matrix.os }}"]
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]

    steps:
      - name: Check out zenoh-kotlin
        uses: actions/checkout@v4
        with:
          path: zenoh-kotlin

      # The pinned commit lives in one file at the repository root, so moving it
      # is a whole-file overwrite - the shape eclipse-zenoh/ci's lockfile sync
      # already uses for Cargo.lock, and what lets a bot maintain this pin
      # instead of a human editing it per upstream fix.
      - name: Read the zenoh-flat-jni pin
        id: flat-jni
        working-directory: zenoh-kotlin
        run: |
          set -euo pipefail
          ref=$(tr -d '[:space:]' < zenoh-flat-jni.pin)
          if [[ ! "$ref" =~ ^[0-9a-f]{40}$ ]]; then
            echo "::error::zenoh-flat-jni.pin must hold a full commit SHA, got '$ref'"
            exit 1
          fi
          echo "ref=$ref" >> "$GITHUB_OUTPUT"

      - name: Check out zenoh-flat-jni
        uses: actions/checkout@v4
        with:
          repository: eclipse-zenoh/zenoh-flat-jni
          ref: ${{ steps.flat-jni.outputs.ref }}
          # zenoh-flat is not checked out at all: zenoh-flat-jni resolves it
          # from git, so its Cargo.lock is the rev that decides.
          path: zenoh-flat-jni

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 11

      # zenoh-flat-jni pins its toolchain in rust-toolchain.toml. Install it
      # from that directory, so a missing toolchain fails here rather than
      # inside the Gradle build - and so this repo never has to name a version
      # its dependency is free to change.
      - name: Install Rust toolchain
        working-directory: zenoh-flat-jni
        run: rustup show

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Gradle Test
        working-directory: zenoh-kotlin
        # CI builds against the sibling checkout above, so it opts into the
        # composite build. A release does not: it resolves zenoh-flat-jni from
        # Maven Central like any other consumer.
        #
        # No cargo step precedes this: the composite build's test task depends
        # on zenoh-flat-jni's own native build, so Gradle drives cargo.
        run: ./gradlew jvmTest --info -PuseLocalFlatJni=true

  markdown_lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: DavidAnson/markdownlint-cli2-action@v18
        with:
          config: '.markdownlint.yaml'
          globs: '**/README.md'

  # NOTE: In GitHub repository settings, the "Require status checks to pass
  # before merging" branch protection rule ensures that commits are only merged
  # from branches where specific status checks have passed. These checks are
  # specified manually as a list of workflow job names. Thus we use this extra
  # job to signal whether all CI checks have passed.
  ci:
    name: CI status checks
    runs-on: ubuntu-latest
    needs: [build, markdown_lint]
    if: always()
    steps:
      - name: Check whether all jobs pass
        run: echo '${{ toJson(needs) }}' | jq -e 'all(.result == "success")'

  # Publish snapshot packages. Both coordinates come from one Gradle
  # invocation — see .github/workflows/publish.yml.
  publish_snapshot_package:
    name: Publish snapshot package
    if: contains(fromJSON('["refs/heads/main"]'), github.ref)
    needs: ci
    uses: ./.github/workflows/publish.yml
    permissions:
      contents: read
      packages: write
    with:
      snapshot: true
      branch: ${{ github.ref_name }}
    secrets: inherit
